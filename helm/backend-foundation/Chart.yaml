apiVersion: v2
name: backend-foundation
description: Helm chart untuk backend-foundation (API + worker BullMQ + migration hook) — Phase 20 Enterprise DevOps.
type: application
# `version` (versi CHART) dan `appVersion` (versi APLIKASI, lihat
# `package.json`) SENGAJA angka terpisah — chart bisa naik versi
# (mis. menambah field values.yaml baru) tanpa aplikasi ikut naik
# versi, dan sebaliknya.
version: 0.1.0
appVersion: '1.0.0'
keywords:
  - nodejs
  - typescript
  - express
  - prisma
  - bullmq
maintainers:
  - name: Platform Team
